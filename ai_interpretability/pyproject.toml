[project]
name = "interpretability"
version = "0.1.0"
description = "ML interpretability experiments for Distinction Structures"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "torch>=2.0.0",
    "scikit-learn>=1.0.0",
]
